export interface CityPoint {
  name: string;
  x: number;
  y: number;
}

export enum PointType {
  start = '#939F9B',
  end = '#a6f2a5',
  past = '#939F9B',
  far = '#e0a1a1',
  current = '#939F9B',
}
